/**
 * Action codes used in RBAC permission checks.
 * Must match the backend ACTION enum exactly.
 */
export const ACTIONS = {
  CREATE: "CREATE",
  VIEW: "VIEW",
  EDIT: "EDIT",
  DELETE: "DELETE",
};

export default ACTIONS;
